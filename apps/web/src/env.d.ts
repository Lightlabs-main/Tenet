// The Codama-generated error module guards its messages with
// `process.env.NODE_ENV`. Browsers have no `process`; vite.config.ts replaces
// the expression at build time. This declaration only makes that type-check.
declare const process: { env: { NODE_ENV?: string } };
