/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getI64Decoder, getI64Encoder, getStructDecoder, getStructEncoder, getU16Decoder, getU16Encoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';

export const REDEMPTION_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([112, 75, 232, 189, 22, 114, 156, 203]);

export function getRedemptionDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(REDEMPTION_DISCRIMINATOR); }

export type Redemption = { discriminator: ReadonlyUint8Array; circle: Address; owner: Address; seq: bigint; sharesRedeemed: bigint; totalSharesAtSnapshot: bigint; assetsRemaining: number; initiatedAt: bigint; 
/**
 * After this, anyone may force-complete the remaining reservations so one
 * absent participant cannot freeze execution indefinitely (R-20).
 */
reservationDeadline: bigint; 
/**
 * `circle.asset_bitmap` at initiation: exactly the vaults this exit covers.
 * Only those may be reserved. Without it a vault added AFTER initiation
 * (permissionless, and empty) could be reserved in place of a real asset,
 * completing the exit with a real asset never reserved — a third party
 * could do that to grief an exiter.
 *
 * A bitmap, not a count: `CircleAsset.index` is the asset's index in the
 * MANDATE, so vaults need not exist for a contiguous `0..count`. With a
 * count, a Circle holding only asset 1 would reject asset 1 (`1 < 1`), the
 * exit could never complete, and every later exit and epoch would block.
 */
assetBitmapAtSnapshot: number; bump: number;  };

export type RedemptionArgs = { circle: Address; owner: Address; seq: number | bigint; sharesRedeemed: number | bigint; totalSharesAtSnapshot: number | bigint; assetsRemaining: number; initiatedAt: number | bigint; 
/**
 * After this, anyone may force-complete the remaining reservations so one
 * absent participant cannot freeze execution indefinitely (R-20).
 */
reservationDeadline: number | bigint; 
/**
 * `circle.asset_bitmap` at initiation: exactly the vaults this exit covers.
 * Only those may be reserved. Without it a vault added AFTER initiation
 * (permissionless, and empty) could be reserved in place of a real asset,
 * completing the exit with a real asset never reserved — a third party
 * could do that to grief an exiter.
 *
 * A bitmap, not a count: `CircleAsset.index` is the asset's index in the
 * MANDATE, so vaults need not exist for a contiguous `0..count`. With a
 * count, a Circle holding only asset 1 would reject asset 1 (`1 < 1`), the
 * exit could never complete, and every later exit and epoch would block.
 */
assetBitmapAtSnapshot: number; bump: number;  };

/** Gets the encoder for {@link RedemptionArgs} account data. */
export function getRedemptionEncoder(): FixedSizeEncoder<RedemptionArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['circle', getAddressEncoder()], ['owner', getAddressEncoder()], ['seq', getU64Encoder()], ['sharesRedeemed', getU64Encoder()], ['totalSharesAtSnapshot', getU64Encoder()], ['assetsRemaining', getU16Encoder()], ['initiatedAt', getI64Encoder()], ['reservationDeadline', getI64Encoder()], ['assetBitmapAtSnapshot', getU16Encoder()], ['bump', getU8Encoder()]]), (value) => ({ ...value, discriminator: REDEMPTION_DISCRIMINATOR }));
}

/** Gets the decoder for {@link Redemption} account data. */
export function getRedemptionDecoder(): FixedSizeDecoder<Redemption> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['circle', getAddressDecoder()], ['owner', getAddressDecoder()], ['seq', getU64Decoder()], ['sharesRedeemed', getU64Decoder()], ['totalSharesAtSnapshot', getU64Decoder()], ['assetsRemaining', getU16Decoder()], ['initiatedAt', getI64Decoder()], ['reservationDeadline', getI64Decoder()], ['assetBitmapAtSnapshot', getU16Decoder()], ['bump', getU8Decoder()]]);
}

/** Gets the codec for {@link Redemption} account data. */
export function getRedemptionCodec(): FixedSizeCodec<RedemptionArgs, Redemption> {
    return combineCodec(getRedemptionEncoder(), getRedemptionDecoder());
}

export function decodeRedemption<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<Redemption, TAddress>;
export function decodeRedemption<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<Redemption, TAddress>;
export function decodeRedemption<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<Redemption, TAddress> | MaybeAccount<Redemption, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getRedemptionDecoder());
}

export async function fetchRedemption<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Redemption, TAddress>> {
  const maybeAccount = await fetchMaybeRedemption(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeRedemption<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Redemption, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeRedemption(maybeAccount);
}

export async function fetchAllRedemption(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Redemption>[]> {
  const maybeAccounts = await fetchAllMaybeRedemption(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeRedemption(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Redemption>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeRedemption(maybeAccount));
}

export function getRedemptionSize(): number {
  return 117;
}