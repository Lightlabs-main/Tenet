/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBooleanDecoder, getBooleanEncoder, getBytesDecoder, getBytesEncoder, getI64Decoder, getI64Encoder, getStructDecoder, getStructEncoder, getU16Decoder, getU16Encoder, getU32Decoder, getU32Encoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { getCircleStateDecoder, getCircleStateEncoder, type CircleState, type CircleStateArgs } from '../types';

export const CIRCLE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([27, 59, 8, 117, 62, 199, 222, 252]);

export function getCircleDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(CIRCLE_DISCRIMINATOR); }

export type Circle = { discriminator: ReadonlyUint8Array; mandate: Address; state: CircleState; createdAt: bigint; totalShares: bigint; 
/**
 * Derived aggregate of `Σ over open epochs (reserved − settled)`.
 *
 * Makes INV-001 checkable in a single account read instead of iterating
 * epochs — which matters because it is asserted after every step of the
 * fuzz harness. Not a second source of truth: the property test recomputes
 * the per-epoch sum and asserts the two agree (decision A-14).
 */
reservedShares: bigint; currentEpoch: bigint; 
/**
 * Number of Members with a non-zero share balance. This is updated on
 * zero→positive settlement and positive→zero redemption transitions.
 */
memberCount: number; assetCount: number; 
/**
 * Redemptions whose reservations are incomplete. Execution is frozen while
 * this is non-zero — but note the freeze alone is NOT sufficient: spending
 * must also respect `balance − reserved` (`REVIEW.md` H-01).
 */
pendingReservations: number; 
/**
 * Set while a NAV snapshot is open, so vault balances cannot move
 * underneath a valuation in progress.
 */
executionFrozen: boolean; bump: number; 
/**
 * Bump of the `VaultAuthority` PDA, which signs every vault transfer.
 * Stored so each signing does not re-derive it.
 */
vaultAuthorityBump: number; 
/**
 * USDC promised to exiting members and not yet claimed — an OBLIGATION,
 * not a balance (the vault's own `amount` stays authoritative). Every
 * debit of the active USDC vault must use `amount − usdc_reserved_raw`
 * (REVIEW.md H-01). Asset vaults carry the same on `CircleAsset`.
 */
usdcReservedRaw: bigint; 
/**
 * Bit `i` set when the vault for Mandate asset index `i` exists. Exits
 * snapshot this to know exactly which vaults they cover.
 */
assetBitmap: number;  };

export type CircleArgs = { mandate: Address; state: CircleStateArgs; createdAt: number | bigint; totalShares: number | bigint; 
/**
 * Derived aggregate of `Σ over open epochs (reserved − settled)`.
 *
 * Makes INV-001 checkable in a single account read instead of iterating
 * epochs — which matters because it is asserted after every step of the
 * fuzz harness. Not a second source of truth: the property test recomputes
 * the per-epoch sum and asserts the two agree (decision A-14).
 */
reservedShares: number | bigint; currentEpoch: number | bigint; 
/**
 * Number of Members with a non-zero share balance. This is updated on
 * zero→positive settlement and positive→zero redemption transitions.
 */
memberCount: number; assetCount: number; 
/**
 * Redemptions whose reservations are incomplete. Execution is frozen while
 * this is non-zero — but note the freeze alone is NOT sufficient: spending
 * must also respect `balance − reserved` (`REVIEW.md` H-01).
 */
pendingReservations: number; 
/**
 * Set while a NAV snapshot is open, so vault balances cannot move
 * underneath a valuation in progress.
 */
executionFrozen: boolean; bump: number; 
/**
 * Bump of the `VaultAuthority` PDA, which signs every vault transfer.
 * Stored so each signing does not re-derive it.
 */
vaultAuthorityBump: number; 
/**
 * USDC promised to exiting members and not yet claimed — an OBLIGATION,
 * not a balance (the vault's own `amount` stays authoritative). Every
 * debit of the active USDC vault must use `amount − usdc_reserved_raw`
 * (REVIEW.md H-01). Asset vaults carry the same on `CircleAsset`.
 */
usdcReservedRaw: number | bigint; 
/**
 * Bit `i` set when the vault for Mandate asset index `i` exists. Exits
 * snapshot this to know exactly which vaults they cover.
 */
assetBitmap: number;  };

/** Gets the encoder for {@link CircleArgs} account data. */
export function getCircleEncoder(): FixedSizeEncoder<CircleArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['mandate', getAddressEncoder()], ['state', getCircleStateEncoder()], ['createdAt', getI64Encoder()], ['totalShares', getU64Encoder()], ['reservedShares', getU64Encoder()], ['currentEpoch', getU64Encoder()], ['memberCount', getU32Encoder()], ['assetCount', getU16Encoder()], ['pendingReservations', getU32Encoder()], ['executionFrozen', getBooleanEncoder()], ['bump', getU8Encoder()], ['vaultAuthorityBump', getU8Encoder()], ['usdcReservedRaw', getU64Encoder()], ['assetBitmap', getU16Encoder()]]), (value) => ({ ...value, discriminator: CIRCLE_DISCRIMINATOR }));
}

/** Gets the decoder for {@link Circle} account data. */
export function getCircleDecoder(): FixedSizeDecoder<Circle> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['mandate', getAddressDecoder()], ['state', getCircleStateDecoder()], ['createdAt', getI64Decoder()], ['totalShares', getU64Decoder()], ['reservedShares', getU64Decoder()], ['currentEpoch', getU64Decoder()], ['memberCount', getU32Decoder()], ['assetCount', getU16Decoder()], ['pendingReservations', getU32Decoder()], ['executionFrozen', getBooleanDecoder()], ['bump', getU8Decoder()], ['vaultAuthorityBump', getU8Decoder()], ['usdcReservedRaw', getU64Decoder()], ['assetBitmap', getU16Decoder()]]);
}

/** Gets the codec for {@link Circle} account data. */
export function getCircleCodec(): FixedSizeCodec<CircleArgs, Circle> {
    return combineCodec(getCircleEncoder(), getCircleDecoder());
}

export function decodeCircle<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<Circle, TAddress>;
export function decodeCircle<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<Circle, TAddress>;
export function decodeCircle<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<Circle, TAddress> | MaybeAccount<Circle, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getCircleDecoder());
}

export async function fetchCircle<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Circle, TAddress>> {
  const maybeAccount = await fetchMaybeCircle(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeCircle<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Circle, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeCircle(maybeAccount);
}

export async function fetchAllCircle(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Circle>[]> {
  const maybeAccounts = await fetchAllMaybeCircle(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeCircle(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Circle>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeCircle(maybeAccount));
}

export function getCircleSize(): number {
  return 96;
}